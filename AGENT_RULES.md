# กฎการเขียน/แก้โค้ดของ AI Agent

ทำตามกฎนี้ทุกครั้งที่ได้รับงานเกี่ยวกับโค้ด — ห้ามข้าม

## 1. เข้าใจก่อนลงมือ (สำคัญที่สุด)
- **สำรวจก่อน**: ใช้ `list_dir` ดูโครงสร้างโฟลเดอร์ก่อนเสมอ
- **อ่านก่อนแก้**: `read_file` อ่านไฟล์ที่จะแก้ให้ครบ (และไฟล์ที่เกี่ยวข้อง เช่น ตัวที่ import/เรียกใช้) ก่อนเขียนโค้ดใดๆ ห้ามเดาเนื้อหาไฟล์
- ถ้ามีไฟล์กฎในโปรเจกต์ (`AGENT_RULES.md`, `CLAUDE.md`, `RULES.md`, `CONTRIBUTING.md`, `README.md`) ให้อ่านก่อน แล้วทำตาม
- ถ้าโจทย์กว้าง/กำกวม ให้ `notify` สรุปสิ่งที่เข้าใจ + สมมติฐาน + แผน ก่อนลงมือ

## 2. แก้ให้น้อยที่สุด (minimal diff)
- แก้เฉพาะสิ่งที่โจทย์ต้องการ **ห้ามเขียนไฟล์ใหม่ทั้งไฟล์** ถ้าแก้แค่บางบรรทัดได้
- **ห้ามลบ/แก้โค้ดที่ไม่เกี่ยวกับงาน** (คอมเมนต์, ฟังก์ชันอื่น, format เดิม)
- รักษา style เดิมของไฟล์: การตั้งชื่อ, indent (space/tab), quote, ความหนาแน่นของคอมเมนต์
- ไม่จัด format ใหม่ทั้งไฟล์ ไม่เรียงลำดับ import ใหม่ ถ้าไม่ได้ถูกสั่ง

## 3. เขียนโค้ดให้ถูกและปลอดภัย
- จัดการ error + edge case (ค่าว่าง, null, input ผิด, ไฟล์ไม่มี)
- **อย่าเพิ่ม dependency ใหม่** ถ้าเขียนเองไม่กี่บรรทัดได้ หรือ stdlib ทำได้
- ใช้ของที่โปรเจกต์มีอยู่แล้ว (ฟังก์ชัน/ไลบรารี/pattern เดิม) ก่อนสร้างใหม่
- ห้ามแตะไฟล์ลับ/คอนฟิก (`.env`, key, token, credential) เว้นแต่ถูกสั่งชัดเจน

## 4. ตรวจสอบเสมอ (ห้ามส่งงานที่ยังไม่เช็ค)
- แก้เสร็จต้องตรวจว่าไม่พัง (ถ้าเปิดสิทธิ์รันคำสั่ง):
  - JS/TS: `node --check <file>` หรือ build/lint ของโปรเจกต์
  - Python: `python -m py_compile <file>`
  - ภาษาอื่น: คอมไพล์/รันเทสต์ที่มี
- ถ้ามีเทสต์ ให้รันเทสต์ที่เกี่ยว
- รายงานผลตามจริง: ถ้าเทสต์ fail ให้บอก + แสดง output ไม่ปั้นว่าสำเร็จ

## 5. งานยาว/ซับซ้อน
- แตกเป็นขั้นย่อย ทำทีละขั้น ตรวจทีละขั้น
- งาน build/ติดตั้ง/สคริปต์ยาว → ใช้ `bg_start` เป็น background แล้ว `bg_status` เช็คความคืบหน้า
- `notify` อัปเดตความคืบหน้าเป็นระยะ

## 6. สื่อสาร
- ตอบสรุปสั้น กระชับ เป็นภาษาไทย
- บอกว่าแก้ไฟล์ไหน บรรทัดไหน ทำอะไร เพราะอะไร
- ถ้าทำบางส่วนไม่ได้/ข้ามไป ต้องบอกตรงๆ ว่าอะไรยังไม่เสร็จ

## เช็คลิสต์ก่อนบอกว่า "เสร็จ"
1. อ่านไฟล์ที่เกี่ยวครบแล้ว?
2. แก้เฉพาะที่จำเป็น ไม่แตะของอื่น?
3. ตรวจ syntax/รันเทสต์ผ่าน?
4. สรุปสิ่งที่ทำ + ไฟล์ที่แก้ชัดเจน?
